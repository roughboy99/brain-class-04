# Telegram slide notes

1. **Title:** Keep this deck as the only shared window during Telegram setup.
2. **Class 3 stays untouched:** Treat `/ask` as a stable contract and use the Ask tab to split faults.
3. **Threat model:** A public bot username can front private documents; the guard runs first and fails closed.
4. **Hard gates:** Public HTTPS is mandatory. The original localhost assumption was wrong.
5. **BotFather mock:** Never open the real BotFather conversation on the recording.
6. **Private step:** Leave this slide visible while the hidden-input setup runs in an unshared terminal.
7. **Compose correction:** `.env` is interpolation input, not an automatic container environment. Recreate n8n.
8. **Webhook lifecycle:** `409` is temporary; removing the webhook restores `getUpdates`.
9. **Credential form:** n8n is on the shared screen for this step; the terminal is not. Paste into the password field, never click the reveal icon, save, watch the green test, clear the clipboard. The bot on camera is disposable and revoked afterwards.
10. **Workflow map:** The pre-class JSON is an inactive, unverified study copy.
11. **Release gate:** A second-account silence test is mandatory before the bot remains active.
12. **Troubleshooting:** Identify the boundary before changing tokens or rebuilding nodes.
13. **Finish:** Promise the behavior demonstrated, not a timing guarantee.

